/*
 * General Messages
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.utils';

export default defineMessages({
  disabledEditTitle: {
    id: `${scope}.disabledEditTitle`,
    defaultMessage: 'Edit capability is disabled in this environment.',
  },
});

/*
 * Site Card messages
 * This contains all the text for the SiteCard.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SiteCardComponent';

export default defineMessages({
  icianLabel: {
    id: `${scope}.icianLabel`,
    defaultMessage: 'ICIAN',
  },
});

/*
 * System diagram auto sync dc bus messages
 *
 * This contains all the text for the auto sync dc bus.
 */
import { defineMessages } from 'react-intl';

export const scope =
  'app.containers.SitesPage.SiteDetails.Overview.SystemDiagram.AutoSyncDCBus';

export default defineMessages({
  autoSyncDCBusTitle: {
    id: `${scope}.autoSyncDCBus.title`,
    defaultMessage: 'auto-sync dc-bus',
  },
});
